{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "default",
  "description": "Capabilities for the main Helm window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:window:allow-start-dragging",
    "core:webview:default",
    "core:event:default",
    "core:app:default",
    "updater:default",
    "process:allow-restart"
  ]
}
